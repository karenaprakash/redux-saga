import React from 'react';
import LoginContainer from '../../../Containers/Admin/Login/login_container';

const AdminLogin = (props) => {
    return (
        <div>
            <LoginContainer {...props}  />
        </div>
    )
}

export default AdminLogin;